/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
      "./index.html", // Add paths to all your HTML files
      "./src/**/*.{js,jsx,ts,tsx}", // Add paths to all JS/JSX files in src/
    ],
    theme: {
      extend: {
        colors: {
        },
      },
    },
    plugins: [],
}  